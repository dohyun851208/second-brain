---
name: second-brain
description: 교사의 LLM 대화 로그와 문서에서 판단 기준을 추출하고, 성취기준·지침 같은 남의 규범 자료와 서명·신분증·통장사본·증명사진 같은 개인정보 이미지를 컴파일과 분리해 문서·양식 작성에 안전하게 재사용하는 Second Brain 볼트를 설치·컴파일·유지한다. Use when the user says "세컨드브레인" 또는 "세컨드 브레인"(시작·설치·초기화·세팅·셋팅·파싱·증류·컴파일·갱신·업그레이드·복구·상태·규범자료·참고자료·성취기준·개인정보 이미지·서명·통장사본·신분증·증명사진·일반 이미지·사진 삽입·양식 작성), "second brain", "나를 닮은 검수 AI", "검수 두뇌", "내 판단 기준 추출해줘", or asks to set up, parse, compile, update, cite curriculum standards or official guidelines from the vault, fill a form with approved image assets, insert a raster image at a measured position in an existing HWP/HWPX document, or check the bundled kordoc compatibility of a Second Brain vault (0_원본/1_파싱/2_두뇌/5_규범자료/6_참고자료/7_개인정보이미지/8_시스템/9_형식 구조의 옵시디언 볼트).
---

# Second Brain

LLM 대화 로그(원본)에서 사용자의 판단 기준·수정 패턴·선택 방식을 증류해, 새 작업에서 "일반적으로 좋은 답"이 아니라 **"이 사람이라면 선택할 답"**을 만드는 개인화 볼트 시스템.

층위: `0_원본/`(**내가 만든** raw) → `1_파싱/`(md 변환본 — kordoc 파싱·OCR) → `2_두뇌/`(증류된 판단 규칙 + 팩트카드). 서명·도장·신분증·통장사본·증명사진처럼 원형을 재사용할 이미지는 컴파일 밖의 `7_개인정보이미지/`. 지휘자는 `AGENTS.md`/`CLAUDE.md`, 유지보수는 `8_시스템/`, 문서 산출·파싱 엔진은 `9_형식/`.

**남이 만든 자료는 증류하지 않는다.** 남의 판단이 두뇌에 섞이면 "이 사람이라면 선택할 답"이 무너지기 때문이다. 대신 파싱만 해서 두 층에 둔다 — 내 말로 바꾸면 틀린 게 되는 것(성취기준·지침·법령)은 `5_규범자료/`, 재구성해도 되는 것(교재·남의 수업안·연수 자료)은 `6_참고자료/`. 5·6층 파싱본은 `1_파싱/`으로 내보내지 않고 원본 옆에 둔다 (증류 입력층과 계약이 섞이지 않게).

## 모드 판별 (호출되면 가장 먼저)

현재 작업 폴더(cwd)의 상태를 보고 모드를 고른다:

| 상태 | 모드 |
|---|---|
| `AGENTS.md` 없음 + Second Brain 구조 없음 | 초기화 |
| `AGENTS.md` 없음 + `2_두뇌/` 또는 `8_시스템/` 있음 | 복구 (`AGENTS.md`·`CLAUDE.md` 재설치) |
| `AGENTS.md` 있음 + `시스템 버전` 표식 없음 | 다른 프로젝트의 지침 파일일 수 있으므로 건드리지 않고 충돌 보고 |
| `1_두뇌/` 있음 + `2_두뇌/` 없음 | v1 구조 → 업그레이드(마이그레이션) 제안 |
| `2_두뇌/` 있음 + `5_규범자료/` 없음 | v2 구조 → 업그레이드(5·6층 추가) 제안 |
| 구조 있음 + `2_두뇌/index.md` 반영 대장에 없거나 수정시각이 달라진 `0_원본/` 파일 있음 | 파싱 → 이어서 증류 제안 |
| 구조 있음 + `5_규범자료/`·`6_참고자료/`에 짝 파싱본 없는 파일 있음 | **파싱만** 제안 (증류 대상 아님) |
| 구조 있음 + 대장에 파싱본만 있고 반영처가 빈 항목 있음 | 증류(컴파일) |
| 구조 있음 + 새·수정 파일 없음 | 상태보고 |
| 사용자가 "파싱" 요청 | 파싱만 실행 |
| 사용자가 "업그레이드" / "복구" 요청 | 업그레이드·복구 |

## 초기화

전제: **현재 작업 폴더(cwd)를 볼트로 사용한다.** 개인 이름이나 특정 컴퓨터의 절대경로를 추측하거나 저장하지 않는다.

1. 사용자에게 이름(호칭)을 한 번 묻는다.
2. **비파괴 원칙**: 없는 폴더·파일만 생성한다. 기존 파일은 수정·이동·삭제하지 않는다. 기존 노트가 많은 볼트면 시스템 파일만 추가하고, 원본으로 쓸 파일을 `0_원본/`에 **복사**하라고 안내한다.
3. 이 스킬의 `templates/` 내용물을 볼트 최상위에 복사한다. `templates/AGENTS.md` → `볼트/AGENTS.md`, `templates/CLAUDE.md` → `볼트/CLAUDE.md`는 선택 항목이 아니라 **설정 완료의 필수 파일**이다.
4. 새로 만든 `AGENTS.md`의 `{{이름}}`을 치환한다. 이미 다른 내용의 `AGENTS.md`가 있으면 덮어쓰거나 자동 병합하지 말고, 다른 볼트 폴더를 고르거나 명시적으로 병합할지 사용자에게 묻는다.
5. `AGENTS.md`·`CLAUDE.md`가 실제로 존재하는지 확인한 뒤 `8_시스템/log.md`에 setup 항목을 기록한다.
6. 안내한다: "`0_원본/`에 **내가 만든** 것(LLM 대화 내보내기·이력서·내 수업안·내 글, hwp·pdf·엑셀 뭐든)을 넣고 '컴파일해줘'라고 하세요. 형식 문서는 자동으로 md로 파싱된 뒤 증류됩니다. 이후 일상 작업은 스킬 없이도 이 폴더에서 자동으로 작동합니다."
   덧붙인다: "**남이 만든 자료는 증류하지 않습니다** — 성취기준·교육청 지침처럼 한 글자도 바꾸면 안 되는 것은 `5_규범자료/`, 교재·남의 수업안처럼 참고만 할 것은 `6_참고자료/`에 넣으세요. 내용을 읽을 이미지는 `0_원본/`, 문서에 그대로 넣을 서명·신분증·통장사본·증명사진은 `7_개인정보이미지/`에 넣으세요."

## 파싱 · 증류(컴파일)

두 단계로 나뉜다. **정본은 볼트 안에 있다** (스킬에 사본을 두지 않는 이유: 볼트가 자급자족이어야 Codex 등 다른 도구에서도 동일하게 작동한다):

1. **파싱** — `8_시스템/파서-프롬프트.md`. 모든 형식(hwp·hwpx·pdf·docx·xls·xlsx·png·jpg·webp·md·txt)을 md로 기계 변환한다. 스캔 PDF와 이미지는 kordoc의 로컬 OCR을 사용한다. 사용자가 변환 품질을 검수할 수 있는 중간 지점. 출력 위치는 출신에 따라 다르다: `0_원본/`→`1_파싱/`, `5_규범자료/`·`6_참고자료/`→**원본 옆 같은 폴더**. 파싱본 이름은 원본의 확장자를 `.md`로 교체하며(`수업안.hwp`→`수업안.md`), 같은 폴더에서 이름이 충돌할 때만 `수업안(pdf).md`로 구분한다.
2. **증류** — `8_시스템/컴파일러-프롬프트.md`. `1_파싱/`의 md만 입력으로 판단 규칙을 추출한다 (선별 → 분석 → 임시 요약 → 확인 질문 → 보정 → 두뇌 기록 → 색인·log 갱신).

"컴파일해줘"는 두 단계 연속 실행 — 미파싱 원본이 있으면 파싱부터, 이어서 증류. "파싱해줘"는 1단계만.
`5_규범자료/`·`6_참고자료/`는 **파싱까지만** 하고 증류하지 않으며, index 대장에 등록하지 않고 각 폴더 `자료목록.md`를 갱신한다 (대장에 넣으면 반영처가 영원히 비어 증류 필요로 오인된다).
`7_개인정보이미지/`는 두 단계 모두의 입력이 아니며 탐색·OCR·대장 등록·두뇌 반영에서 제외한다.

**권장 사고 수준**: 파싱은 낮음(절차·도구가 표로 고정돼 판단할 것이 없다). 증류는 확인 질문·보정 단계(3~4단계)에 최대 — 경쟁 가설을 세우고 갈라지는 지점을 찾는 작업이라 사고를 아끼면 단일 확인형 질문으로 무너진다. 나머지 증류 단계는 중간, 기록·색인 갱신은 낮음.

## HWP/HWPX 원칙

- 일반적인 HWP/HWPX 읽기·파싱·수정·양식 채우기·생성·비교·검증·미리보기는 `9_형식/references/kordoc.md`의 **Kordoc 경로만** 사용한다.
- Kordoc 4.x는 지원되는 HWP/HWPX/HWP3 암호 문서를 `--password`로 읽는다. 암호를 응답·로그·파싱본에 남기지 않는다. 한컴 DRM, 손상 파일, 지원하지 않는 구조처럼 Kordoc이 안전하게 처리하지 못하는 입력은 원본을 그대로 두고 실패 이유만 보고한다. 아래 승인 이미지 배치 예외가 아니면 COM 변환, 직접 XML 편집, pack/unpack, 다른 생성기로 자동 우회하지 않는다.
- 새 HWPX를 만드는 중 마크다운 이미지 참조를 넣을 때는 Kordoc `generate --image-dir`를 사용한다. **유일한 예외는 기존 HWP/HWPX의 지정 위치에 사용자가 지정했거나 양식상 필요성이 확인되고 사용 승인된 래스터 이미지를 실측 배치하는 작업**이다. 서명·도장뿐 아니라 신분증·통장사본·증명사진·일반 사진도 `9_형식/scripts/place_image.py`로 넣는다. Kordoc으로 텍스트 작업을 먼저 끝낸 뒤 이미지를 마지막에 배치한다. `.hwpx`는 바로 처리하고, `.hwp`는 이 스크립트가 한컴 COM으로 임시 HWPX를 정확히 한 번 만든 뒤 같은 경로를 실행한다. `place_signature.py`는 기존 명령 호환용이며, 내부 `hwp_to_hwpx.py`·`insert_signature_hwpx.py`를 직접 실행하거나 XML을 즉석 편집하지 않는다.
- 이미지 경로의 트리거는 최초 요청에 "서명"이나 "이미지"라는 단어가 있었는지가 아니라 **최종 제출본에 이미지 삽입이 필요한가와 사용 범위가 승인됐는가**다. 양식 사전점검이나 작업 중 필수 이미지·서명·날인란을 발견하면 이 경로로 라우팅한다. `7_개인정보이미지/` 자산이 아직 미승인이면 자산과 용도를 한 번 확인하고, 승인되면 같은 작업을 계속한다. 사용자가 특정 문서와 이미지 삽입을 직접 지정했다면 그 범위는 승인된 것으로 본다. 필수란을 말없이 비운 채 완성본으로 보고하지 않는다.
- `.hwp` 변환은 이 승인 이미지 배치 작업 안에서만 허용한다. 원본 HWP는 보존하고 최종본은 반드시 `*_완성본.hwpx`로 내며, 완성본을 COM으로 다시 저장하거나 `.hwp`로 되돌리지 않는다. Windows 한컴·`pywin32`·PyMuPDF가 없거나 변환·실측이 실패하면 서명은 Kordoc `seal`로, 일반 이미지는 다른 임의 경로로 우회하지 말고 필요한 조건을 알린다.

## 규범·참고 자료 (5·6층)

- **판별은 "누가 만들었나"로 한다.** 사용자가 만든 것은 아무리 길어도 `0_원본/`(증류함). 남이 만든 것은 `5_규범자료/`(내 말로 바꾸면 틀린 게 되는 것) 또는 `6_참고자료/`(재구성해도 되는 것)이며 어느 쪽도 증류하지 않는다.
- **규범 인용은 없으면 안 쓴다.** 성취기준 코드·법령 조항·지침 문장의 직접 인용이 필요하면 `5_규범자료/자료목록.md`에서 해당 **범위**를 확인한다. 없으면 지어내지 말고 사용자에게 묻는다 — 자료를 넣어줄지, 그 칸만 비울지. "국어과는 있는데 5~6학년군이 없다"도 없는 것이다. 이 문은 대조하면 맞다/틀리다가 나오는 값에만 걸고, 활동 설계·평가 방법 같은 일반 서술에는 걸지 않는다.
- **출처 표시.** 5층 인용에는 출처를 붙여 원문 대조가 가능함을 보이고, 6층을 반영한 부분에는 출처와 함께 "두뇌 규칙 근거 없음"을 밝힌다. 두뇌 규칙은 짧고 참고자료 원문은 길어서, 표시가 없으면 양이 많은 쪽이 판단을 밀어내고도 티가 나지 않는다.
- **인용은 현행판에서만.** `_지난판/`은 사용자가 과거 문서 검증을 명시적으로 요청할 때만 연다.
- 5층은 요약본을 만들지 않는다 (요약이 폴더에 있으면 언젠가 원문 대신 인용된다). 목차까지만 허용. 6층은 요약 허용이되 `2_두뇌/`가 아닌 그 폴더 안에 둔다.
- 5·6층은 민감정보가 아니므로 열람 승인이 필요 없다.
- `6_참고자료/`를 쓴 세션에서 무엇을 채택·폐기했는지는 사용자의 판단이므로 `0_원본/` 저장 후보로 제안한다 — 남의 자료가 두뇌를 키우는 경로다.

## 개인정보 이미지 · 양식 작성

- **읽을 이미지 → `0_원본/`, 형태 그대로 재사용할 개인정보 이미지 → `7_개인정보이미지/`**로 라우팅한다. 일회성 비민감 일반 사진은 `7_개인정보이미지/`로 옮기지 않고 사용자가 지정한 경로에서 삽입한다.
- 특정 양식 작성 요청에서는 쓰기 전에 필수·선택 항목과 이미지 칸·별도 첨부·서명·날인란을 전체 점검하고, 대상 양식, 관련 `2_두뇌/` 모듈, `7_개인정보이미지/자산목록.md`, `9_형식/라우팅.md`를 결합한다.
- 먼저 양식을 분석하고 자산 목록에서 필요한 후보만 고른다. 이미지 폴더 전체를 열거나 일괄 OCR하지 않는다.
- 일반적인 작성 요청은 개인정보 자산 후보 제안까지로 본다. 필수 개인정보 이미지·서명·날인란을 발견하면 완료 전에 사용할 자산과 용도를 한 번 확인하며, 승인 뒤 같은 작업을 계속한다. 선택·모호한 이미지란은 자동 삽입하지 않는다. 사용자가 특정 문서와 자산의 삽입을 명시했다면 그 범위는 이미 승인된 것으로 본다. 양식이 별도 파일 첨부를 요구하면 문서 안 이미지 삽입으로 대체하지 않는다.
- 원본 이미지는 불변이다. 변형은 작업용 사본이나 출력 문서 내부에서만 하고, 완료 후 민감 값 없이 `7_개인정보이미지/사용기록.md`에 기록한다.

## 상태보고

두뇌 버전(`2_두뇌/index.md`)·시스템 버전(`AGENTS.md` 주석)·모듈 현황·미파싱/미반영 원본을 요약 보고하고, kordoc 실행 버전과 호환 메이저도 점검한다. 필요 시 `8_시스템/갱신절차.md`의 lint 패스를 제안한다.

## 업그레이드 · 복구

- 이 스킬 `templates/AGENTS.md`의 시스템 버전과 볼트 `AGENTS.md`의 시스템 버전을 비교한다.
- 스킬이 더 새 버전이면 `8_시스템/`(log.md·가설-기록.md 제외)·`9_형식/`·구조 파일의 갱신을 제안하고, 동의받아 교체한다. 볼트에 없는 새 시스템 파일(예: 2.1의 `8_시스템/가설-기록.md`)은 빈 템플릿으로 생성한다.
  새 `7_개인정보이미지/` 구조가 없으면 빈 템플릿으로 추가한다. `0_원본/`에 원형 재사용 개인정보 이미지로 보이는 파일이 있으면 사용자에게 분류·이동을 제안하며 자동 이동하지 않는다.
  **`0_원본/`, `1_파싱/`, `2_두뇌/`, `5_규범자료/`, `6_참고자료/`, `7_개인정보이미지/`의 사용자 내용, `log.md`·`가설-기록.md`·자산 사용 기록은 동의 없이 건드리지 않는다.**
- 시스템 파일은 새 정본을 추가·갱신하되, 이전 버전의 미참조 파일이나 사용자가 만든 파일을 자동 삭제하지 않는다. 새 라우팅에서 쓰지 않는 파일은 그대로 두고 무시한다.
- 복구: 시스템 파일이 사라졌으면 `templates/`에서 해당 파일만 다시 복사한다.
- 관리자 규칙: `templates/`의 시스템 파일을 수정하면 반드시 `templates/AGENTS.md`의 시스템 버전을 올린다 — 업그레이드 감지는 이 숫자 비교로만 작동한다.
- kordoc 호환 범위의 정본은 `templates/9_형식/references/kordoc.md`의 실행 명령(현재 `@^4`, 마지막 검증판 `4.9.2`)이다. 상태·업그레이드 요청 때 `npx -y kordoc@^4 --version`과 `npm view kordoc version`을 비교한다. 엔진 소스는 스킬에 복사해 내장하지 않는다. 필요하면 공식 저장소를 임시 폴더에 받아 마지막 검증 태그 이후의 변경 기록·CLI 계약만 비교한다.
  - 최신판이 같은 메이저면 실행 시 자동 적용된다. 새 기능이나 동작 변경이 파싱 절차에 영향을 줄 때만 참조·파서·라우팅과 시스템 버전을 함께 갱신한다.
  - 같은 메이저라도 반영을 마치면 `kordoc.md`의 마지막 검증판을 올리고 대표 문서 스모크 테스트 결과를 남긴다.
  - 더 높은 메이저가 나와도 범위를 자동으로 올리지 않는다. 공식 변경 기록 검토와 대표 문서 스모크 테스트 후 호환 범위·지침·시스템 버전을 함께 갱신한다.

### v1 → v2 마이그레이션 (폴더 재배치)

볼트에 `1_두뇌/`가 있고 `2_두뇌/`가 없으면 v1 구조다. 동의받아 순서대로:

1. 폴더 이름 변경(내용 불변): `3_형식`→`9_형식`, `2_시스템`→`8_시스템`, `1_두뇌`→`2_두뇌` — 반드시 이 순서(이름 충돌 방지)
2. `1_파싱/` 생성, 스킬 `templates/1_파싱/_안내.md` 복사
3. 볼트 내 md 파일들의 경로 문자열 일괄 갱신: `1_두뇌/`→`2_두뇌/`, `2_시스템/`→`8_시스템/`, `3_형식/`→`9_형식/` (규칙의 `0_원본/` 출처 링크는 그대로 유효 — 새 규칙부터 파싱본을 출처로 쓴다)
4. `2_두뇌/index.md` 반영 대장에 파싱 열(파싱본·파싱일)을 추가한다. 이미 반영된 원본은 다음 파싱 때 파싱본만 만들어 대장을 채운다 — 반영처가 이미 있으므로 재증류하지 않는다
5. 시스템 파일(`AGENTS.md`·`8_시스템/`·`9_형식/`)을 새 템플릿으로 교체({{이름}} 재치환), `log.md`에 마이그레이션 기록

### v2 → v3 (규범·참고 층 추가)

볼트에 `2_두뇌/`는 있는데 `5_규범자료/`가 없으면 v2 구조다. 동의받아:

1. `5_규범자료/`·`6_참고자료/`를 템플릿(`_안내.md`·`자료목록.md`)으로 생성한다. 기존 폴더 내용은 건드리지 않는다.
2. `0_원본/`에 남이 만든 자료로 보이는 파일(성취기준·지침·교재·남의 수업안 등)이 있으면 **목록으로 제시하고 이동을 제안**한다. 자동 이동하지 않는다 — 오판 시 규칙의 출처 링크가 끊긴다. 이동에 동의하면 원본·파싱본·index 대장 항목을 함께 정리하고, 그 원본에서 나온 기존 규칙이 있으면 사용자에게 유지 여부를 묻는다.
3. `2_두뇌/`에 남의 자료를 요약한 지식 모듈(v2.0의 ④ 유형 산출물)이 있으면 `6_참고자료/`로 이동을 제안하고 `index.md`에서 등록을 해제한다.
4. 기존 파싱본 이름이 옛 규칙(`수업안.hwp.md`)이면 새 규칙(`수업안.md`)으로 개명할지 묻는다. 동의하면 파일·`<이름>.images/` 폴더·index 대장 경로·규칙의 출처 링크를 함께 갱신한다. 거부하면 그대로 두고 새로 파싱하는 것부터 새 규칙을 적용한다 (혼재해도 동작에는 문제없다).
5. 시스템 파일을 새 템플릿으로 교체({{이름}} 재치환), `log.md`에 마이그레이션 기록.

## 원칙

- 몰래 두뇌를 바꾸지 않는다 — 감지는 자동, 파싱·증류 실행은 사용자 승인 후.
- 수집은 수동이 원칙 — 세션 로그를 자동 투입하지 않는다. 사람의 선별 자체가 첫 번째 품질 필터다.
- 원본은 불변 — 이름·내용·위치를 바꾸지 않는다. 5·6층 원본도 같다.
- 파싱본은 재생성 가능한 파생물 — 정본은 언제나 원본이다. 손으로 고치지 않는다.
- **남의 판단은 증류하지 않는다** — 5·6층은 파싱만 한다. 두뇌층에 남의 자료 요약 모듈을 만들지 않는다. 이 층에 있는 것이 전부 사용자의 판단이어야 "이 사람이라면 선택할 답"이 성립한다.
- **규범은 인출하지 생성하지 않는다** — 성취기준 코드·조항·지침 인용문은 `5_규범자료/`에 실물이 있을 때만 쓰고, 없으면 지어내지 말고 묻는다.
- 개인정보 이미지 원본도 불변이며 `7_개인정보이미지/`에만 둔다. 컴파일하지 않고 문서 작업에 승인된 범위에서만 사용한다.
- 개인정보는 볼트에만 존재한다. 이 스킬 저장소에는 어떤 개인 데이터도 넣지 않는다.
- HWP/HWPX 일반 작업과 새 문서의 이미지 임베드는 Kordoc 단일 경로를 유지하고, 기존 문서에 승인된 래스터 이미지를 지정 위치에 넣는 작업의 `.hwp`→임시 `.hwpx` 변환과 실측 배치만 예외 처리한다.
- 초기화 이후의 일상 운영(라우팅·검수·컴파일)은 스킬 없이 볼트의 `AGENTS.md`/`CLAUDE.md`만으로 작동한다.
